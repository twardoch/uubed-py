"""Setup script for the uubed-py package.

This script uses setuptools to define the package metadata and dependencies,
allowing for proper installation and distribution of the `uubed-py` library.
"""
from setuptools import setup

setup()